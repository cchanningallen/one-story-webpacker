import { randomPalette } from './colors';

export const currentPalette = randomPalette();

export const themeConfig = {};

export const spacing = {
  none: 0,
  xs: 8,
  sm: 16,
  md: 24,
  lg: 40,
};
